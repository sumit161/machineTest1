import { ShowClassDirective } from './show-class.directive';

describe('ShowClassDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowClassDirective();
    expect(directive).toBeTruthy();
  });
});
